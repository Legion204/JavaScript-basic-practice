var names = 'riyad'
var place =['dhaka', 'mars', 'gabtoli']
var food = ['tea', 'meat', 80, true]

console.log(Array.isArray(names))
console.log(Array.isArray(food))

const all = food.concat(place) // to connect 2 array
console.log(all)
